<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Added permissions for sensitive features -->
    <uses-permission android:name="android.permission.READ_CONTACTS" /> <!-- For contacts access -->
<!--    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" /> &lt;!&ndash; For DND/notification policy access &ndash;&gt;-->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" /> <!-- For mobile data toggling (limited) -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" /> <!-- For airplane mode/settings changes (older APIs) -->
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" tools:ignore="ProtectedPermissions" /> <!-- For secure settings like airplane mode (requires system/root) -->

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="true" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera.flash"/>
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

<!--    <activity android:name="com.spotify.sdk.android.authentication.LoginActivity"-->
<!--        android:exported="true"-->
<!--        android:theme="@android:style/Theme.Translucent.NoTitleBar"-->
<!--        tools:ignore="MissingClass">-->
<!--        <intent-filter>-->
<!--            <action android:name="android.intent.action.VIEW" />-->
<!--            <category android:name="android.intent.category.DEFAULT" />-->
<!--            <category android:name="android.intent.category.BROWSABLE" />-->
<!--            <data android:scheme="myapp" android:host="callback" />-->
<!--        </intent-filter>-->
<!--    </activity>-->

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NEON"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>



    </application>

</manifest>